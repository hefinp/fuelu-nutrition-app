## Packages
framer-motion | Essential for smooth, premium entry animations and interactions
recharts | Beautiful, clean minimal charts for the macro nutrient breakdown
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility to merge tailwind classes safely

## Notes
- Tailwind Config needs extending for custom fonts (font-display, font-sans).
- The `numeric` columns for weight/height in Postgres return strings; ensure the form coercion matches the Drizzle schema.
